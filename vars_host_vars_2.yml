# This playbook shows simple variables example. Variables are taking from the web file in the host_vars dir.

---
- name: Simple variable example
  hosts: 192.168.1.60
  tasks:

    - name: Install software
      yum:
        name: "{{ packages }}"
        state: present
...
